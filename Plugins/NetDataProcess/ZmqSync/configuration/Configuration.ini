[ZmqSync]
Save = true

; 每个配制组 对应一个 Worker 运行于独立的线程中
; 每个配制组 必须包含一个或者多个 Topic, 用于从 PUB 端订阅
; 每个配制组 必须包含一个 PUB 端的地址, 用于连接
; 实例如下
; [Worker-xxx]
; Topics = xxx,xxx
; ServerUrl = tcp://192.168.1.xxx:xxxx
